{
  "name": "byte_blog",
  "author": "mathmorais",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:production": "docker-compose up",
    "client:dev": "yarn workspace @byte_blog/client start:development",
    "client:production": "yarn workspace @byte_blog/client start:production",
    "server:dev": "yarn workspace @byte_blog/server start:development",
    "server:production": "docker-compose up -d server",
    "test": "jest"
  },
  "start:devDependencies": {
    "@byte_blog/eslint-config": "*",
    "@types/jest": "^26.0.20",
    "jest": "^26.6.3",
    "prettier": "^2.2.1",
    "ts-jest": "^26.5.2",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  }
}
